[project]
name = "stock-value-analyzer"
version = "0.1.0"
description = "Evidence-first long-term stock analysis tool using SEC filings."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
stock-value-analyzer = "stock_value_analyzer.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
